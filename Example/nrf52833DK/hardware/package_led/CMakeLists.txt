# CMakeLists.txt for package_led
# if(CONFIG_PACKAGE_LED)
#     target_sources(app PRIVATE src/app_led.c)
# endif()

# # expose the header files of package_led to the whole project
# zephyr_include_directories(inc)