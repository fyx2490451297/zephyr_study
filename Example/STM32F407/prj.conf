# 启用日志功能
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y

# 启用 GPIO
CONFIG_GPIO=y

# ================================
# 自定义模块配置区
# ================================
# 开启LED
CONFIG_PACKAGE_LED=y
# 开启后台自动闪烁，并设置频率为 500ms
CONFIG_PACKAGE_LED_AUTO_BLINK=y
CONFIG_PACKAGE_LED_BLINK_INTERVAL=500